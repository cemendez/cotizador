info:
  name: Crear PDF aceptada
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/quotes/{{quoteId}}/contract"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
