info:
  name: Obtener cliente de otro usuario
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/quotes/{{clientId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
