info:
  name: Cambio en valor
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/quotes/{{quoteId}}"
  body:
    type: json
    data: |-
      {
        "taxRate": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
