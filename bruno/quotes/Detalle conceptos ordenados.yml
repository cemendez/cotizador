info:
  name: Detalle conceptos ordenados
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/quotes/{{quoteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
