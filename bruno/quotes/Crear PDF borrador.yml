info:
  name: Crear PDF borrador
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/quotes/{{quoteId}}/pdf"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
