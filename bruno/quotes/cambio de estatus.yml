info:
  name: cambio de estatus
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}/quotes/{{quoteId}}/status"
  body:
    type: json
    data: |-
      {
        "status": "SENT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
