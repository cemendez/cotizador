info:
  name: Obtener borradores
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/quotes?status=DRAFT"
  params:
    - name: status
      value: DRAFT
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
