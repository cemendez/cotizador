info:
  name: Obtener usuario
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
