info:
  name: Búsqueda
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/clients?search=ber"
  params:
    - name: search
      value: ber
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
